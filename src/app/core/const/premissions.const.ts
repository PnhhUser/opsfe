// hardcode permissions
const view = 'view';
// app
const modules = `${view}.modules`;
const settings = `${view}.settings`;
const dashboard = `${view}.dashboard`;
const analytics = `${view}.analytics`;

// modules
const hr_management = `${view}.hr_management`;
const access_control = `${view}.access_control`;
const customers_management = `${view}.customers`;
const storagies = `${view}.storagies`;

const accounts = `${view}.accounts`;
const positions = `${view}.positions`;
const departments = `${view}.departments`;
const employees = `${view}.employees`;

const roles = `${view}.roles`;
const permissions = `${view}.permissions`;
const roleSetup = `${view}.role_setup`;

export const PERMISSIONS = {
  view,
  modules,
  settings,
  dashboard,
  analytics,
  hr_management,
  access_control,
  customers_management,
  storagies,
  accounts,
  positions,
  departments,
  employees,
  roleSetup,
  roles,
  permissions,
};
